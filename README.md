# AES-Crypt-Brute-Force-Tool
Try passwords from a wordlist (e.g., rockyou.txt) to decrypt .aes files created with AES-Crypt. This tool uses pyAesCrypt and performs efficient, line-by-line password attempts with safe file handling, resume support, and progress bars.
